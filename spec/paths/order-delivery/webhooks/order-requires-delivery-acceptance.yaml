post:
  tags:
  - order-delivery-webhooks
  summary: Order requires delivery acceptance
  description: ""
  consumes: []
  produces:
  - application/json
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: array
          items:
            "$ref": "#/definitions/connectorder"
  responses:
    '200':
      description: OK