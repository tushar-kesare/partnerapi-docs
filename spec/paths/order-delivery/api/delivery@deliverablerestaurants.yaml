get:
  tags:
  - Order Delivery API
  summary: Get deliverable restaurants
  parameters:
  - name: toLat
    in: query
    required: false
    type: number
    format: double
  - name: toLon
    in: query
    required: false
    type: number
    format: double
  - name: toPostcode
    in: query
    required: false
    type: string
  responses:
    '200':
      description: OK
      schema:
        "$ref": "#/definitions/deliverable-restaurants"
  deprecated: true
