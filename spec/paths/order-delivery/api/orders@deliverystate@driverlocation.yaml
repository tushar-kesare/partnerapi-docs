put:
  tags:
  - order-delivery-api
  summary: Current driver location (bulk upload)
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: array
          items:
            "$ref": "#/definitions/bulk-delivery-details"
  responses:
    '200':
      description: OK
